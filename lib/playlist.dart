import 'package:flutter/material.dart';

class Playlist extends StatelessWidget {
  Playlist({Key? key, this.nowPlaying, this.play, this.openPlayer})
      : super(key: key);
  var nowPlaying;
  var play;
  var openPlayer;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
